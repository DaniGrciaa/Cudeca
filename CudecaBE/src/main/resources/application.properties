spring.application.name=CudecaBE

# --- BASE DE DATOS (RED PRIVADA) ---
# Usamos la URL interna de Railway. Es mas rapida y segura.
spring.datasource.url=jdbc:postgresql://postgres.railway.internal:5432/railway
# Railway inyecta PGUSER y PGPASSWORD automaticamente en las variables de entorno
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# --- SOLUCION SSL PARA RED PRIVADA (.internal) ---
# IMPORTANTE: Al usar la red interna, el modo 'require' falla el handshake.
# Debemos desactivarlo. La seguridad la da la propia red de Railway.
spring.datasource.hikari.data-source-properties.ssl=false
spring.datasource.hikari.data-source-properties.sslmode=disable

# --- FLYWAY (MIGRACIONES) ---
spring.flyway.enabled=true
# baseline-on-migrate ayuda si la BD ya tenia tablas creadas manualmente
spring.flyway.baseline-on-migrate=true
spring.flyway.baseline-version=1
spring.flyway.validate-on-migrate=true
spring.flyway.out-of-order=false
spring.flyway.clean-disabled=true

# --- JPA / HIBERNATE ---
# validate: Verifica que Flyway creo las tablas correctamente
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# --- PUERTO ---
server.port=${PORT:8080}

# --- SEGURIDAD Y JWT ---
jwt.secret=${JWT_SECRET}
jwt.expiration=36000000
jwt.refresh.expiration=604800000

# --- OAUTH2 (Google/Facebook) ---
app.oauth2.redirect-uri=${FRONTEND_URL}/oauth2/redirect

# Google
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}
spring.security.oauth2.client.registration.google.scope=email,profile
spring.security.oauth2.client.registration.google.redirect-uri=${BACKEND_URL}/login/oauth2/code/google

# Facebook (Opcional, asegurate de tener las variables si lo usas)
spring.security.oauth2.client.registration.facebook.client-id=${FACEBOOK_CLIENT_ID}
spring.security.oauth2.client.registration.facebook.client-secret=${FACEBOOK_CLIENT_SECRET}
spring.security.oauth2.client.registration.facebook.scope=email,public_profile
spring.security.oauth2.client.registration.facebook.redirect-uri=${BACKEND_URL}/login/oauth2/code/facebook